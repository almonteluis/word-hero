// Global styles have moved to src/app.css (Tailwind + design tokens).
// This component is kept as a no-op so existing imports don't break.
const GlobalStyles = () => null;

export default GlobalStyles;
